services:
  - type: web
    name: autojoinbot
    runtime: python

    plan: free

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python bot.py

    healthCheckPath: /health

    autoDeploy: true
